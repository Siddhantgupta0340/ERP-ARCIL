'use client';
import { Badge, Panel } from '@/components/ui';
import { useWorkflowItems } from '@/lib/workflow-store';
import { money } from '@/lib/utils';

export default function MatchingPage() {
  const { items } = useWorkflowItems();

  return <div className="space-y-5"><Panel title="Phase 3: 3-way matching" subtitle="PO details, GRN/delivery challan, and invoice details are compared before approval."><div className="grid gap-3 md:grid-cols-3"><div className="rounded-2xl border border-white/10 bg-slate-950/45 p-4"><div className="text-xs uppercase tracking-[0.18em] text-slate-500">Matched</div><div className="mt-2 text-2xl font-semibold text-white">{items.filter((item) => item.matchStatus === 'Matched').length}</div></div><div className="rounded-2xl border border-white/10 bg-slate-950/45 p-4"><div className="text-xs uppercase tracking-[0.18em] text-slate-500">Variance</div><div className="mt-2 text-2xl font-semibold text-white">{items.filter((item) => item.matchStatus === 'Variance').length}</div></div><div className="rounded-2xl border border-white/10 bg-slate-950/45 p-4"><div className="text-xs uppercase tracking-[0.18em] text-slate-500">Pending</div><div className="mt-2 text-2xl font-semibold text-white">{items.filter((item) => item.matchStatus === 'Pending').length}</div></div></div></Panel><Panel title="PO / GRN / Invoice comparison table" subtitle="Quantity, amount, and status differences are visible without opening a card detail view."><div className="overflow-auto"><table className="min-w-[1150px] w-full border-separate border-spacing-0 text-left text-sm"><thead><tr className="text-xs uppercase tracking-[0.14em] text-slate-500"><th className="border-b border-white/10 px-3 py-3">Invoice</th><th className="border-b border-white/10 px-3 py-3">PO amount / qty</th><th className="border-b border-white/10 px-3 py-3">GRN qty</th><th className="border-b border-white/10 px-3 py-3">Invoice amount</th><th className="border-b border-white/10 px-3 py-3">Amount variance</th><th className="border-b border-white/10 px-3 py-3">Qty variance</th><th className="border-b border-white/10 px-3 py-3">Outcome</th><th className="border-b border-white/10 px-3 py-3">Hold needed</th><th className="border-b border-white/10 px-3 py-3">Next route</th></tr></thead><tbody>{items.map((item) => { const amountVariance = item.invoiceAmount - item.poAmount; const qtyVariance = item.grnQty - item.poQty; return <tr key={item.id} className="hover:bg-white/[0.03]"><td className="border-b border-white/5 px-3 py-4 font-medium text-white">{item.invoiceNumber}<div className="text-xs text-slate-500">{item.vendorName}</div></td><td className="border-b border-white/5 px-3 py-4 text-slate-300">{money(item.poAmount)} / {item.poQty}</td><td className="border-b border-white/5 px-3 py-4 text-slate-300">{item.grnQty}</td><td className="border-b border-white/5 px-3 py-4 text-slate-200">{money(item.invoiceAmount)}</td><td className="border-b border-white/5 px-3 py-4 text-slate-300">{money(amountVariance)}</td><td className="border-b border-white/5 px-3 py-4 text-slate-300">{qtyVariance}</td><td className="border-b border-white/5 px-3 py-4"><Badge tone={item.matchStatus === 'Matched' ? 'emerald' : item.matchStatus === 'Variance' ? 'amber' : 'slate'}>{item.matchStatus}</Badge></td><td className="border-b border-white/5 px-3 py-4"><Badge tone={item.matchStatus === 'Variance' ? 'amber' : 'emerald'}>{item.matchStatus === 'Variance' ? 'Yes' : 'No'}</Badge></td><td className="border-b border-white/5 px-3 py-4"><Badge tone={item.approvalLevel === 'L1' ? 'cyan' : item.approvalLevel === 'L2' ? 'violet' : 'amber'}>{item.approvalLevel}</Badge></td></tr>; })}</tbody></table></div></Panel></div>;
}
